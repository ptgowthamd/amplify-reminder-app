type Reminder @model @auth(rules: [{allow: owner, ownerField: "userId", identityClaim: "sub"}]) {
  id: ID! @primaryKey
  userId: String! @index(name: "byUser", sortKeyFields: ["remindAt"])
  title: String!
  description: String!
  remindAt: AWSDateTime!
  stepFnExecutionArn: String
  }
 
